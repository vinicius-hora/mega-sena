server:
  port: 4001
spring:
  profiles:
    active: dev
---
#configura��o de versionamento
spring:
  liquibase:
    enabled: false
    change-log:
      chasspath: db/changelog/db-liquibase.xml

#variaveis de ambiente
token:
  api-megasena: ${API_MEGASEMA}

url:
  api-loteria: ${URL-API-MEGASENA:https://apiloterias.com.br/app/}
#
#log FeignClient
logging.level.com.estudo.megasena.repository.*: DEBUG
---
spring:
  cloud:
    openfeign:
      client:
        config:
          feignName:
            encoder: com.estudo.megasema.dto.resultados.ResultadoDto
            decoder: com.estudo.megasema.dto.resultados.ResultadoDto
#            contract: com.example.SimpleContract
#            capabilities:
#              - com.example.FooCapability
#              - com.example.BarCapability
#            queryMapEncoder: com.example.SimpleQueryMapEncoder
#            micrometer.enabled: false