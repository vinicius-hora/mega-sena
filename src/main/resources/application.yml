server:
  port: 4001
spring:
  profiles:
    active: dev
---
#configura��o de versionamento
spring:
  liquibase:
    enabled: false
    change-log:
      chasspath: db/changelog/db-liquibase.xml

token:
  api-mega-sena: ${API-MEGASEMA:}
